let arr = ["1","2","3"];
let strlist = '';

arr.forEach((item) => {
  alert(`${item}`);
    let itemSet = document.querySelector ('elem');
    strlist = strlist+`<div>${item}</div>`;
    elem.innerHTML = strlist;
});
